{
  "name": "casaos",
  "version": "v0.4.2",
  "private": true,
  "scripts": {
    "preinstall": "npx only-allow yarn",
    "dev": "turbo run serve",
    "dev:noCache": "turbo run serve --force --cache-dir ./.turbo/cache",
    "build": "turbo run build"
  },
  "packageManager": "yarn@3.6.0",
  "workspaces": [
    "main",
    "packages/*"
  ],
  "dependencies": {
    "@vue/composition-api": "^1.7.1",
    "csslint": "^1.0.5",
    "htmlhint": "^1.1.4",
    "jshint": "^2.13.6",
    "jsonlint": "^1.6.3"
  },
  "devDependencies": {
    "turbo": "1.10.3"
  },
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not dead"
  ]
}
